import { Text, View } from 'react-native';

interface Props {}

const Counter = (props: Props) => {
  const { rerwwr } = props;
  return (
    <View>
      <Text>Hola Nare</Text>
    </View>
  );
};

export default Counter;
